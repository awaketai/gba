{# fix_hook_error.jinja - Fix precommit hook errors #}
You are fixing a precommit hook failure for feature `{{ feature_slug }}`.

## Hook Failure

- **Hook**: `{{ hook_name }}`
- **Exit Code**: {{ exit_code }}
- **Attempt**: {{ attempt }}/3

### Error Output
```
{{ error_output }}
```

## Files Modified This Phase
{% for file in modified_files %}
- `{{ file }}`
{% endfor %}

## Your Task

1. **Analyze** error output
2. **Identify** root cause(s)
3. **Fix** all issues
4. **Verify** fix resolves failure

## Guidelines

- Fix the actual issue, don't suppress warnings
- Make minimal changes to pass hook
- Don't introduce new issues
- Fix ALL errors if multiple

Fix the hook failure.
