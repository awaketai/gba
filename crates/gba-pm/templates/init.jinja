{# init.jinja - Project initialization #}
You are initializing GBA (Geektime Bootcamp Agent) for this project.

## Your Task

Initialize the current repository for GBA by performing these steps:

### 1. Create Directory Structure

Create the following directories and files:

```
.gba/
├── config.yml          # Project configuration
└── features/           # Feature specs directory (empty for now)

.trees/                 # Code tree cache (will be gitignored)
```

**Create `.gba/config.yml`** with this content:
```yaml
# GBA Project Configuration

project:
  name: "{{ project_name }}"
  initialized_at: "{{ timestamp }}"

agent:
  model: "claude-sonnet-4-20250514"
  max_tokens: 8192

hooks:
  build: true
  fmt: true
  lint: true
  security_check: true
  custom: []

execution:
  auto_commit: true
  auto_review: true
  max_fix_attempts: 3
  create_pr: true

verification:
  run_tests: true
  check_coverage: true
  coverage_threshold: 80
```

### 2. Update .gitignore

Add to `.gitignore` (create if not exists):

```
# GBA - Code tree cache
.trees/
```

Note: Do NOT gitignore `.gba/` - it should be committed.

### 3. Analyze Repository

Analyze the codebase and identify:
- Primary programming language(s)
- Build system and package manager
- Directory structure and key modules

### 4. Generate Documentation

For each important directory, create a `.gba.md` file with:
- Purpose of the directory
- Key files and responsibilities
- Dependencies and relationships

### 5. Create/Update CLAUDE.md

Create or update `CLAUDE.md` in project root with:
- Project overview
- Architecture summary (ASCII diagram)
- Technology stack
- References to `.gba.md` files
- Development conventions

## Output

Provide summary of:
1. Directories created
2. Files generated
3. Key codebase insights
