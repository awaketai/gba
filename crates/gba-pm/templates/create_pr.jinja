{# create_pr.jinja - Create Pull Request #}
You are creating a Pull Request for feature `{{ feature_slug }}`.

## Feature Summary

- **Feature**: `{{ feature_slug }}`
- **Branch**: `feature/{{ feature_slug }}`
- **Base**: `main`

## Completed Phases
{% for p in completed_phases %}
### Phase {{ loop.index }}: {{ p.name }}
- Commit: `{{ p.commit }}`
- Goal: {{ p.goal }}
{% endfor %}

## Verification Results

- Tests: {{ verification.tests_passed }}/{{ verification.tests_total }} passed
- Coverage: {{ verification.coverage }}%
- Lint: {{ "Passed" if verification.lint_passed else "Failed" }}
- Security: {{ "Passed" if verification.security_passed else "Failed" }}

## Your Task

Create PR using `gh` CLI:

### 1. Push Branch
```bash
git push -u origin feature/{{ feature_slug }}
```

### 2. Create PR
```bash
gh pr create --base main --head feature/{{ feature_slug }} --title "{{ title }}" --body "$(cat <<'EOF'
## Summary

{{ description }}

## Changes

{% for p in completed_phases %}
- **{{ p.name }}**: {{ p.summary }}
{% endfor %}

## Implementation Highlights

- [Key architectural decisions]
- [Notable patterns used]
- [Trade-offs made]

## Files Changed

<details>
<summary>{{ files_changed | length }} files</summary>

{% for f in files_changed %}
- `{{ f }}`
{% endfor %}

</details>

## Testing

- [x] Unit tests: {{ verification.tests_passed }}/{{ verification.tests_total }}
- [x] Coverage: {{ verification.coverage }}%
- [x] Lint: Passed
- [x] Security: Passed

## How to Test

1. `git checkout feature/{{ feature_slug }}`
2. [Testing steps]
3. [Expected results]

---
🤖 Generated with GBA
EOF
)"
```

### 3. Output

Report:
- PR number
- PR URL

Create the Pull Request now.
