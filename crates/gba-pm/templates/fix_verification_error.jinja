{# fix_verification_error.jinja - Fix verification failures #}
You are fixing verification failures for feature `{{ feature_slug }}`.

## Failures

{% for f in failures %}
### {{ f.category }}: {{ f.check }}
- **Expected**: {{ f.expected }}
- **Actual**: {{ f.actual }}
{% if f.error %}
- **Error**: {{ f.error }}
{% endif %}

{% endfor %}

## Completed Phases
{% for p in completed_phases %}
- Phase {{ loop.index }}: {{ p.name }} (commit: `{{ p.commit }}`)
{% endfor %}

## Your Task

Fix all failures:

1. **Analyze** why each check failed
2. **Fix** the code (or test if test is wrong)
3. **Re-verify** fixes don't break other tests

## Guidelines

### Test Failures
- Understand expected behavior
- Fix implementation, not just make test pass

### Coverage Issues
- Add meaningful tests for uncovered paths
- Focus on critical paths and edge cases

### Acceptance Criteria
- Review original requirement
- Implement missing functionality

Fix all verification failures.
